version: '3.8'

services:
  backend-auth:
    build: .
    container_name: backend-autenticacion
    ports:
      - "5000:5000"
    environment:
      SECRET_KEY: clave-secreta-dev
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: inventario_db
      DB_USER: admin
      DB_PASSWORD: admin123
    networks:
      - red-inventario

networks:
  red-inventario:
    external: true
